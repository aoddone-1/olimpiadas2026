{
    "name": "ipvlapampa/olimpiadas",
    "description": "Sistema de Inscripciones - Olimpiadas IPV La Pampa 2026 (CodeIgniter 3)",
    "type": "project",
    "license": "MIT",
    "require": {
        "php": ">=7.4",
        "tecnickcom/tcpdf": "^6.11"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.6",
        "squizlabs/php_codesniffer": "^3.7"
    },
    "autoload-dev": {
        "classmap": ["tests/support/"]
    },
    "config": {
        "sort-packages": true,
        "optimize-autoloader": true
    },
    "scripts": {
        "test": "phpunit --colors=always",
        "test:coverage": [
            "@putenv XDEBUG_MODE=coverage",
            "phpunit --colors=always --coverage-text"
        ],
        "lint": "@phpcs",
        "phpcs": "phpcs",
        "phpcbf": "phpcbf"
    },
    "scripts-descriptions": {
        "test": "Ejecuta la suite de tests unitarios (PHPUnit).",
        "lint": "Revisa el estilo de código del proyecto con PHP_CodeSniffer.",
        "phpcbf": "Corrige automáticamente el estilo de código (PHP Code Beautifier)."
    }
}
